# SEO / GSC / GA4 / GEO audit — lizliz.xyz (2026-08-07)

**Live:** https://lizliz.xyz (brief: www → apex)  
**Repo:** `/home/ubuntu/projects/lizliz.xyz`  
**Stack (source of truth):** Next.js 16 App Router, static export → Cloudflare Pages. Brief said Vite+React+TS; that is incorrect — AGENTS.md + `package.json` confirm Next.js.  
**Method:** source-first audit + live `curl` evidence; refinements in source only. **No commit / no push.**

## Live evidence (2026-08-07)

| URL | Result |
|---|---|
| `https://lizliz.xyz/` | 200; title/desc/OG/Twitter/canonical present; GA4 `G-TXVLTJJ878` preloaded; Person + WebSite JSON-LD |
| `https://lizliz.xyz/robots.txt` | 200; CF Content-Signals overlay + `Allow: /` + `Sitemap: https://lizliz.xyz/sitemap.xml` |
| `https://lizliz.xyz/sitemap.xml` | 200 `application/xml`; includes home, articles, skills, adventurex, articles/*, podcast/* |
| `https://lizliz.xyz/rss.xml` | 200 |
| `https://lizliz.xyz/og-image.png` | 200 (1200×630 asset present) |
| `https://lizliz.xyz/favicon.svg` | 200 |
| `https://lizliz.xyz/favicon.ico` | **404** (pre-fix) |
| `https://lizliz.xyz/apple-touch-icon.png` | **404** (pre-fix) |
| `https://www.lizliz.xyz/` | **522** (CF DNS resolves; origin/Pages hostname unhealthy) |

## Checklist (GSCG)

| Item | Status | Action taken | File:line |
|---|---|---|---|
| **GSC: sitemap.xml** exists, valid, real URLs, sane lastmod | OK → improved | Source already generated articles + podcasts + skills. Added `lastModified` on static routes. | `src/app/sitemap.ts:8-68` |
| **GSC: robots.txt** allows pages, references sitemap | OK | Source `robots.ts` allow `/` + sitemap URL. Live file is CF-managed Content-Signals + same Sitemap line — OK. | `src/app/robots.ts:7-13` |
| **GSC: verification token** | Absent | Reported only. Prior audit had wrong value (`G-TXVLTJJ878` as google verification) — already removed. **Do not invent.** | — |
| **GSC: canonical** single, https apex | OK | Root + home + articles + per-article/podcast/skills set `alternates.canonical` / metadataBase `https://lizliz.xyz`. | `src/app/layout.tsx:37,106-110`; `src/app/page.tsx:37-39`; `src/app/articles/page.tsx:33-34` |
| **GSC: noindex leak** | OK | Indexable pages `index/follow`. No `src/app/preview` in source (stale `out/preview` only). | layout/page robots blocks |
| **GA4: gtag G-TXVLTJJ878** | OK → hardened | Live already served ID. Source used env-only; added hard fallback so ID stays intact without env. Head async via `next/script` `afterInteractive`. | `src/app/layout.tsx:29-30,217-228` |
| **GA4: ID intact** | OK | Fallback + live = `G-TXVLTJJ878`. Never changed away from house ID. | `src/app/layout.tsx:30` |
| **GEO: entity signals** | OK | Visible tagline (EN/ZH); Person schema (jobTitle, knowsAbout, sameAs); WebSite schema; home/articles ItemList. | `src/app/layout.tsx:114-154`; `src/i18n/en.ts:2-3` |
| **GEO: JSON-LD** | OK → fixed | Person + WebSite sitewide; Article/Breadcrumb per article; PodcastEpisode + Breadcrumb; skills SoftwareApplication-style pages. Fixed podcast breadcrumb fake “Podcast→home” middle crumb (no `/podcast` index). | `src/app/podcast/[slug]/page.tsx:134-147` |
| **GEO: factual / FAQ** | Partial | Articles/podcasts are specific. No sitewide FAQ — not warranted for portfolio home (residual P2). | — |
| **SEO: title ~60** | OK | Root/home `Liz — AI agents, SaaS systems, and writing` (42 chars). Template `%s \| Liz`. | `src/app/layout.tsx:32-40`; `src/app/page.tsx:8-16` |
| **SEO: meta description 120–160** | OK | Site desc 158 chars; articles index 144; home page variant present. Pain-first, no filler rewrite. | `src/app/layout.tsx:33-34`; `src/app/articles/page.tsx:7-8` |
| **SEO: OG + Twitter** | OK → fixed | Site/home/articles have og:image 1200×630 + `summary_large_image`. Podcast previously omitted images when no episode ogImage — now falls back to `/og-image.png`. | `src/app/layout.tsx:71-94`; `src/app/podcast/[slug]/page.tsx:33-68` |
| **SEO: hreflang / lang** | OK (known limit) | `html lang="en"`; client `data-lang` EN/ZH. `/zh/` prefix client-only — fresh `/zh/` 404 known & acceptable per MASTER. | `src/app/layout.tsx:162-194` |
| **SEO: one h1 + hierarchy** | OK | Home single h1 (brand); sections h2. Articles use content heading rules. | `src/app/HomeContent.tsx` |
| **SEO: favicon + apple-touch + theme-color** | Was gap → fixed | Generated `apple-touch-icon.png` (180), `favicon-32x32.png`, `favicon.ico` from existing monogram SVG (no brand swap). Wired icons + light/dark `themeColor`. | `public/apple-touch-icon.png`; `public/favicon.ico`; `src/app/layout.tsx:59-69` |
| **SEO: JSON-LD valid** | OK → fixed | Absolute URLs; no invented GSC token; podcast breadcrumb no longer duplicates Home as “Podcast”. | `src/app/podcast/[slug]/page.tsx:134-147` |

## Changes made (working tree, uncommitted)

1. `src/app/layout.tsx` — GA4 fallback `G-TXVLTJJ878`; `themeColor`; favicon.ico / svg / 32png + apple-touch icons.
2. `public/apple-touch-icon.png`, `public/favicon-32x32.png`, `public/favicon.ico` — derived from `public/favicon.svg` via sharp (same monogram).
3. `src/app/podcast/[slug]/page.tsx` — OG/Twitter image fallback; truthful 2-level BreadcrumbList.
4. `src/app/sitemap.ts` — `lastModified` on static routes.
5. This audit file.

## Verification

**Skipped local `build` / `typecheck`.** Touched metadata + static assets + sitemap route data only — not `index.html` / Vite/webpack/`_headers` build plumbing. Per house A-R6 for CF Pages personal sites: commit+push (Hermes) is primary verify. Residual risk: CF Preview build after ship.

## Residuals

### P0
- **`www.lizliz.xyz` → HTTP 522.** Apex OK; www DNS hits Cloudflare but origin/Pages binding fails. Brief expects www→apex. Fix in Cloudflare Pages custom domains / SSL (attach `www` or redirect), not in app source. Blocks clean www/apex canonical story until fixed.

### P1
- **GSC verification token absent.** Add real Search Console meta or DNS TXT when Liz provides the token — do not invent. Then confirm property ownership + submit `https://lizliz.xyz/sitemap.xml`.
- **Per-article unique OG art** still mostly falls back to site `/og-image.png` (38 articles, 0 frontmatter `ogImage`). Functional but weak social CTR — craft pass residual, not blocking crawl.

### P2
- Sitewide FAQPage schema / FAQ section — skip unless a real Q&A page is written (no fake FAQ).
- PodcastSeries `url` points at site root (no `/podcast` index) — acceptable; optional future index page.
- Live `robots.txt` is CF Content-Signals managed (not raw Next `out/robots.txt` alone). Keep source `robots.ts` in sync; if CF overlay ever drops Sitemap line, restore via Pages/CF settings.
- Stale `out/preview` build artifact in tree — not in `src/`; ignore or clean on next full build.

## Confidence

- **High:** live crawlability (robots/sitemap/rss/og), GA4 presence, title/desc/canonical baseline, icon/theme fixes.
- **Medium:** www 522 is CF-side; GSC ownership unknown without token/dashboard.
- **Owner:** Liz / Hermes for ship + CF www domain + GSC token.
- **Next checkpoint:** after Hermes commit+push — curl apple-touch/favicon.ico on prod; fix www in CF; submit sitemap in GSC.
